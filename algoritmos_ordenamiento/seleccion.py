def seleccion(lista:list):
    """ devuelve una lista ordenada de menor a mayor mediante el algoritmo de ordenamiento por seleccion  """
    vector = lista
    for i in range(0, len(vector)-1):
        min = i
        for k in range()